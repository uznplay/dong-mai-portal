# Mobile Optimization Dependencies
# ==============================

# Core (desktop - để convert model)
torch==1.13.1
onnx==1.14.0
onnxruntime==1.15.1

# Optional: TensorFlow for TFLite conversion
# tensorflow==2.13.0

# Lightweight inference (chạy trên mobile CPU)
opencv-python==4.8.0.74
numpy==1.24.0
Pillow==10.0.0

# Flask API (nếu muốn deploy server)
flask==2.3.0
flask-cors==4.0.0

# Production server
waitress==2.1.0
